services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: my-docker-app:latest
    ports:
      - "3000:3000"
    networks:
      - deployagent_default
networks:
  deployagent_default:
    external: true